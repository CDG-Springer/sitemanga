// // Encontra todos os paginas com a classe 'pagina'
// const paginas = document.querySelectorAll('.pagina');
// const pagina = document.querySelector('.pagina img');

// // Adiciona um event listener para capturar a tecla pressionada
// document.addEventListener('keydown', function(event) {

//     const paginaWidth = pagina.offsetHeight

//     if (event.keyCode === 40) {
//         event.preventDefault();
//         window.scrollBy({
//             top: paginaWidth + 4,
//             behavior: 'smooth'
//         });
//     }
// });

// // Encontra todos os paginas com a classe 'pagina'

// // Adiciona um event listener para capturar a tecla pressionada
// document.addEventListener('keyup', function(event) {

//     const paginaWidth = pagina.offsetHeight

//     console.log(paginaWidth)

//     if (event.keyCode === 38) {
//         event.preventDefault();
//         window.scrollBy({
//             top: -(paginaWidth - 70),
//             behavior: 'smooth'
//         });
//     }
// });
